using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class explanation : MonoBehaviour
{
    public bool Please_Read;

    //This object is used in raycasting - if it's deleted, the raycast will remember what was last looked at, which will screw up the NPC interaction system
}
